name: Build Kernel AOSP Pipa

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip ccache gcc g++ clang lld libncurses-dev bc bison flex libssl-dev

      - name: Clone Clang toolchain
        run: |
          mkdir -p $HOME/tc
          git clone --depth=1 https://github.com/Neutron-Toolchains/clang $HOME/tc

      - name: Set permission to build.sh
        run: chmod +x ./build.sh

      - name: Run build script
        run: ./build.sh

      - name: Upload kernel zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: kernel-flashable-zip
          path: |
            *.zip
